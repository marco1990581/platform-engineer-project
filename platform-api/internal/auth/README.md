## Authentication Architecture

The authentication subsystem has been designed using
separation of responsibilities.

Current components:

- User model
- Repository abstraction
- Authenticator abstraction
- Basic Authentication middleware

Future work:

- JSON user repository
- SQLite backend
- JWT authentication
- Audit logging
